# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'

begin
  require 'bundler'
  Bundler.require
rescue LoadError
end

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'swifty'

  app.cartfile do
    cart 'github "nickoneill/PermissionScope" "master"'
    cart 'github "SwiftyJSON/SwiftyJSON"'
    # cart 'github "MengTo/Spring"'
    # cart 'github "Ramotion/folding-cell"'
    # cart 'github "CosmicMind/Algorithm"'
    # cart 'github "sudeepag/SAConfettiView"'
    # cart 'github "yukiasai/Gecco"'
    # cart 'github "amayne/SwiftString"'
    # cart 'github "FolioReader/FolioReaderKit"'
    # cart 'github "hyperoslo/Hue"'
    # cart 'github "oyvindkg/SwiftyDB"'
    # cart 'github "hyperoslo/ImagePicker"'
    # cart 'github "ReactiveX/RxSwift"'
    # cart 'github "SnapKit/Masonry"'
  end
end
